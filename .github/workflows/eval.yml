name: Lumina Evaluation Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Unit Tests (runs on every push) ────────────────────────────────────────
  unit-tests:
    name: Unit Tests
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Rebuild native modules for Electron
        run: npm run rebuild:native

      - name: Type check
        run: npm run typecheck

      - name: Run unit tests
        run: npm test

  # ── Eval Pipeline (Phase 10 — runs on main branch only) ───────────────────
  # TODO Phase 10: uncomment and implement
  # eval-pipeline:
  #   name: AI Evaluation Pipeline
  #   runs-on: ubuntu-latest
  #   needs: unit-tests
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.11'
  #
  #     - name: Install Python eval dependencies
  #       run: pip install -r tests/eval/requirements.txt
  #
  #     - name: Run RAGAS evaluation
  #       run: python tests/eval/run_eval.py
  #       env:
  #         OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}  # used by RAGAS judge
  #
  #     - name: Upload eval results
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: eval-results-${{ github.sha }}
  #         path: tests/eval/results/
  #
  #     - name: Check thresholds
  #       run: python tests/eval/check_thresholds.py
  #       # Fails CI if:
  #       #   RAGAS faithfulness < 0.83
  #       #   groundedness < 0.86
  #       #   any DeepEval test fails
  #       #   any agent gate test fails
